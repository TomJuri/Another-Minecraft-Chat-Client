/*
 * This file was generated by the Gradle 'init' task.
 *
 * This is a general purpose Gradle build.
 * Learn more about Gradle by exploring our samples at https://docs.gradle.org/7.5-rc-1/samples
 */
 
apply plugin: "java"

compileJava {
	options.encoding = "UTF-8"
}

repositories {
	jcenter()
} 

dependencies {
	implementation "com.google.code.gson:gson:2.3.1"
	implementation "com.flowpowered:flow-nbt:1.0.0"
	implementation fileTree(dir: "lib", include: "*.jar")
}

jar {
	archiveName "AMC-1.8.5.jar"
	manifest {
		attributes "Main-Class": "net.defekt.mc.chatclient.ui.Main"
	}
	
	from {
    	configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
    }
    
    duplicatesStrategy = DuplicatesStrategy.INCLUDE;
}